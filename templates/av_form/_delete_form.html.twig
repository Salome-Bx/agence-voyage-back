<form method="post" action="{{ path('app_av_form_delete', {'id': av_form.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ av_form.id) }}">
        <button class="bg-red-400 rounded p-2 text-white m-2">Supprimer</button>

</form>
