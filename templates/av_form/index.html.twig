{% extends 'base.html.twig' %}

{% block title %}AvForm index{% endblock %}

{% block body %}
    <h1>AvForm index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Message_form</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for av_form in av_forms %}
            <tr>
                <td>{{ av_form.id }}</td>
                <td>{{ av_form.messageForm }}</td>
                <td>
                    <a href="{{ path('app_av_travel_show', {'id': av_travel.id}) }}"   class="bg-blue-400 rounded p-2 m-2 text-white inline">Détails</a>

                    <a href="{{ path('app_av_travel_edit', {'id': av_travel.id}) }}"  class="bg-yellow-400 rounded p-2 m-2 text-white inline">Editer</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_av_form_new') }}">Create new</a>
{% endblock %}
