{% extends 'base.html.twig' %}

{% block title %}AvCategory{% endblock %}

{% block body %}
    <h1>AvCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ av_category.id }}</td>
            </tr>
            <tr>
                <th>Name_category</th>
                <td>{{ av_category.nameCategory }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_av_category_index') }}">back to list</a>

    <a href="{{ path('app_av_category_edit', {'id': av_category.id}) }}">edit</a>

    {{ include('av_category/_delete_form.html.twig') }}
{% endblock %}
