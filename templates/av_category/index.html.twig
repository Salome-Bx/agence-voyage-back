{% extends 'base.html.twig' %}

{% block title %}AvCategory index{% endblock %}

{% block body %}
    <h1>AvCategory index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name_category</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for av_category in av_categories %}
            <tr>
                <td>{{ av_category.id }}</td>
                <td>{{ av_category.nameCategory }}</td>
                <td>
                    <a href="{{ path('app_av_category_show', {'id': av_category.id}) }}">show</a>
                    <a href="{{ path('app_av_category_edit', {'id': av_category.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_av_category_new') }}">Create new</a>
{% endblock %}
