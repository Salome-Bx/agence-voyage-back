{% extends 'base.html.twig' %}

{% block title %}AvStatus index{% endblock %}

{% block body %}
    <h1>AvStatus index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name_status</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for av_status in av_statuses %}
            <tr>
                <td>{{ av_status.id }}</td>
                <td>{{ av_status.nameStatus }}</td>
                <td>
                    <a href="{{ path('app_av_travel_show', {'id': av_travel.id}) }}"   class="bg-blue-400 rounded p-2 m-2 text-white inline">Détails</a>

                    <a href="{{ path('app_av_travel_edit', {'id': av_travel.id}) }}"  class="bg-yellow-400 rounded p-2 m-2 text-white inline">Editer</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_av_status_new') }}">Create new</a>
{% endblock %}
