{% extends 'base.html.twig' %}

{% block title %}AvStatus{% endblock %}

{% block body %}
    <h1>AvStatus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ av_status.id }}</td>
            </tr>
            <tr>
                <th>Name_status</th>
                <td>{{ av_status.nameStatus }}</td>
            </tr>
        </tbody>
    </table>

   <a href="{{ path('app_av_travel_index') }}"  class="bg-blue-400 rounded p-2 m-2 inline text-white">Retour à la liste</a>
   <a href="{{ path('app_av_travel_edit', {'id': av_travel.id}) }}"  class="bg-yellow-400 rounded p-2 m-2 text-white inline">Editer</a>


    {{ include('av_status/_delete_form.html.twig') }}
{% endblock %}
