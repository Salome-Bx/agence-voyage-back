{% extends 'base.html.twig' %}

{% block title %}Edit AvTravel{% endblock %}

{% block body %}
    <h1>Editer un voyage</h1>

    {% for message in app.flashes('success') %}
		<div class="bg-yellow-400 text-black">
			{{ message | join('<br>') }}
		</div>
	{% endfor %}

    {{ include('av_travel/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_av_travel_index') }}"  class="bg-blue-400 rounded p-2 m-2 text-white inline">Retour à la liste</a>

    {{ include('av_travel/_delete_form.html.twig') }}
{% endblock %}
